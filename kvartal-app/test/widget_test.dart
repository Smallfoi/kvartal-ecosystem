import 'package:flutter_test/flutter_test.dart';
import 'package:kvartal_app/main.dart';

void main() {
  test('KvartalApp can be constructed', () {
    expect(const KvartalApp(), isA<KvartalApp>());
  });
}
