# Обмен МАТА ↔ 1С — паспорт интеграции

**Кому:** специалисту по 1С со стороны магазина.
**Состояние на 02.09.2026: приём со стороны МАТА полностью готов и работает на проде.**
От вас нужны выгрузки — адреса, форматы и порядок описаны ниже.

Читать сверху вниз: §1 — что уже есть, §2 — что сделать вам, §3 — как связываемся,
§4–§7 — четыре потока обмена, §8–§13 — правила, проверка и открытые вопросы.

> **Версия для отправки подрядчику** (та же информация, оформленной страницей):
> https://claude.ai/code/artifact/faa8b021-e7d3-444f-a979-41a592b099fd
> Правим этот файл — страницу пересобираем следом, чтобы они не разошлись.

---

## 1. Что уже готово с нашей стороны

Всё перечисленное развёрнуто на боевом сервере, отвечает и покрыто автотестами.
Дорабатывать под вас ничего не требуется — нужно только начать слать данные.

| Поток | Направление | Адрес | Статус |
|---|---|---|---|
| Категории | 1С → МАТА | `POST /v1/integrations/1c/categories` | ✅ работает |
| Карточки товаров | 1С → МАТА | `POST /v1/integrations/1c/catalog` | ✅ работает |
| Цены и остатки | 1С → МАТА | `POST /v1/integrations/1c/prices` | ✅ работает |
| Заказы: забрать | МАТА → 1С | `GET /v1/integrations/1c/orders` | ✅ работает |
| Заказы: подтвердить приём | 1С → МАТА | `POST /v1/integrations/1c/orders/ack` | ✅ работает |
| Заказы: вернуть статус | 1С → МАТА | `POST /v1/integrations/1c/orders/status` | ✅ работает |
| Проверка связи | — | `GET /v1/integrations/1c/status` | ✅ работает |

Базовый адрес: **`https://api.mata-club.ru`**

Что ещё сделано на нашей стороне и вам полезно знать:

- **остаток по размерам** доходит до витрины: размер без остатка покупатель видит
  зачёркнутым и выбрать не может;
- **журнал обмена** в админке магазина: каждая выгрузка оставляет строку — сколько
  получено, добавлено, обновлено, какие были замечания, сколько заняло;
- **защита от повторов**: одна и та же позиция, присланная дважды, не создаёт дубль;
- **пакетная обработка**: выгрузка на тысячи позиций не упирается в таймаут.

---

## 2. Что нужно сделать вам

Никаких эндпоинтов на вашей стороне поднимать **не нужно** — мы понимаем, что 1С
обычно стоит внутри сети и снаружи недоступна. Все запросы делает 1С, мы только
отвечаем. Ваша задача — регламентные задания, которые ходят к нам по HTTPS.

1. **Взять токен обмена** у владельца магазина (постоянный, менять не нужно).
2. **Проверить связь:** `GET /v1/integrations/1c/status` — должен ответить
   `{"status":"ok","enabled":true}`.
3. **Сделать первую полную выгрузку в порядке:** категории → товары → цены.
   Порядок важен, см. §4.
4. **Поставить регламентные задания** по расписанию из §9.
5. **Забирать заказы** и возвращать по ним статусы (§7).
6. Ответить на вопросы из §12.

---

## 3. Как связываемся

| Параметр | Значение |
|---|---|
| Протокол | HTTPS |
| Метод | `POST` (выгрузки), `GET` (проверка связи, заказы) |
| Формат | JSON |
| Кодировка | UTF-8 |
| Заголовок | `Content-Type: application/json` |
| Авторизация | `Authorization: Bearer <токен>` |

**Про токен.** Один постоянный токен на все потоки. Второго фактора нет намеренно:
у робота нет телефона. Вход в админку магазина живёт отдельно и защищён иначе.
Если в настройках токен не задан, приём выключен и любой запрос получит `401` —
это нормальное поведение, а не поломка.

**Тело запроса.** Принимаем и объект, и просто массив — как вам удобнее:

```json
{ "products": [ … ] }        // именованный ключ: categories / products / prices / orders
{ "items": [ … ] }           // или обобщённый
[ … ]                        // или голый массив
```

**Ответ** на любую выгрузку — отчёт о том, что мы приняли:

```json
{ "received": 120, "created": 3, "updated": 117, "keptByOwner": ["price"], "errors": [] }
```

| Код | Что значит | Что делать |
|---|---|---|
| `200` | принято (смотрите отчёт: могут быть замечания в `errors`) | ничего |
| `400` | тело не разобрано — не JSON или не массив | проверить формат |
| `401` | токен неверный или приём выключен | взять токен у владельца |
| `413` | больше 20 000 позиций за раз | слать частями |

Позиция с ошибкой **пропускается, остальные применяются** — одна битая строка не
рушит всю выгрузку. Причина попадает в `errors` и в журнал обмена.

---

## 4. Поток 1 — категории

`POST /v1/integrations/1c/categories`

**Слать перед товарами.** Товар с категорией, которой у нас ещё нет, сохранится, но
не появится ни в одном разделе витрины. Такие категории мы возвращаем в
`unknownCategories` и помечаем обмен «с замечаниями» — но лучше до этого не доводить.

| Поле | Тип | Обяз. | Пример | Описание |
|---|---|:---:|---|---|
| `id` | строка | **да** | `"shoes"` или GUID | ключ категории, неизменный |
| `name` | строка | **да** | `"Кроссовки"` | название для витрины |
| `parentId` | строка | нет | `"clothes"` | родитель, если дерево |
| `sort` | число | нет | `10` | порядок отображения |

```json
{ "categories": [
  { "id": "shoes",   "name": "Кроссовки", "parentId": null, "sort": 10 },
  { "id": "tshirts", "name": "Футболки",  "parentId": null, "sort": 20 }
] }
```

Что важно знать:

- **эмодзи и фото категории — наши.** В 1С их нет, и мы их не затираем при выгрузке;
- **пропавшую из выгрузки категорию мы не удаляем** — на неё ссылаются товары, и
  удаление увело бы с витрины целый раздел. Убрать раздел — осознанное действие
  владельца в админке;
- дерево (`parentId`) мы принимаем и храним, но витрина пока плоская.

---

## 5. Поток 2 — карточки товаров

`POST /v1/integrations/1c/catalog` — меняется редко, отдельный поток.

**Ключ синхронизации — самое важное.** У каждой позиции нужен **неизменный**
идентификатор: GUID номенклатуры (предпочтительно) или артикул. По нему мы находим
товар и обновляем его, а не создаём второй. Идентификатор не должен меняться при
правке карточки в 1С.

| Поле | Тип | Обяз. | Пример | Описание |
|---|---|:---:|---|---|
| `id` | строка | **да** | GUID или `"SS-0042"` | ключ синхронизации |
| `article` | строка | **да** | `"AR-X1-BLK"` | артикул (SKU) |
| `name` | строка | **да** | `"Кроссовки Air Runner X1"` | наименование |
| `categoryId` | строка | **да** | `"shoes"` | ссылка на категорию из §4 |
| `brand` | строка | желат. | `"МАТА"` | бренд |
| `description` | текст | желат. | `"Лёгкие беговые…"` | описание |
| `sizes` | массив строк | да* | `["40","41","42"]` | размеры (*для обуви и одежды) |
| `colors` | массив строк | желат. | `["Чёрный","Белый"]` | цвета |
| `images` | массив URL | желат. | `["https://…/1.jpg"]` | фото, первое — главное |
| `active` | булево | **да** | `true` | в продаже или в архиве |
| `updatedAt` | ISO дата-время | желат. | `"2026-09-01T10:00:00Z"` | для дельта-обмена |

```json
{ "products": [{
  "id": "SS-0042",
  "article": "AR-X1",
  "name": "Кроссовки Air Runner X1",
  "categoryId": "shoes",
  "brand": "МАТА",
  "description": "Лёгкие беговые кроссовки с амортизацией.",
  "sizes": ["40", "41", "42", "43"],
  "colors": ["Чёрный", "Белый"],
  "images": ["https://ваш-хост/AR-X1-1.jpg", "https://ваш-хост/AR-X1-2.jpg"],
  "active": true,
  "updatedAt": "2026-09-01T10:00:00Z"
}] }
```

**Фото передаются ссылками** — файлы мы не забираем. Ссылки должны быть доступны
из интернета без авторизации.

**Снятие с продажи — это `active: false`,** а не удаление позиции из выгрузки.
Пропавший из выгрузки товар мы не трогаем: мало ли почему его не оказалось в пачке.

---

## 6. Поток 3 — цены и остатки

`POST /v1/integrations/1c/prices` — меняется часто, поэтому отдельно от карточек.

| Поле | Тип | Обяз. | Пример | Описание |
|---|---|:---:|---|---|
| `id` | строка | **да*** | `"SS-0042"` | ключ товара (*либо `article`) |
| `article` | строка | да* | `"AR-X1"` | если нет `id` |
| `price` | число | **да** | `11990` | розничная цена |
| `oldPrice` | число | нет | `13990` | старая цена, если есть скидка |
| `stock` | целое | да** | `7` | общий остаток (**либо `variants`) |
| `variants` | массив | лучше | см. ниже | остаток по размерам |

Вариант внутри `variants`: `{ "variantId": "…", "size": "41", "stock": 0 }`.

```json
{ "prices": [{
  "id": "SS-0042",
  "price": 11990,
  "oldPrice": 13990,
  "variants": [
    { "variantId": "SS-0042-40", "size": "40", "stock": 3 },
    { "variantId": "SS-0042-41", "size": "41", "stock": 0 },
    { "variantId": "SS-0042-42", "size": "42", "stock": 7 }
  ]
} ] }
```

Как мы это обрабатываем:

- **`variants` предпочтительнее.** Общий остаток мы посчитаем сами как сумму, а
  размер с нулём покупатель увидит зачёркнутым и выбрать не сможет;
- **один размер в нескольких строках** (разные цвета или склады) — складываем;
- **общий `stock` без `variants` очищает разбивку по размерам.** Это осознанно:
  старая разбивка после такой выгрузки уже неправда;
- **остаток нужен числом**, не «да/нет»;
- товар, которого у нас нет, в этом потоке **не создаётся** — придёт замечание
  «товар не найден». Сначала карточка (§5), потом цена.

---

## 7. Поток 4 — заказы магазина в 1С

Здесь направление обратное, но инициатор всё равно 1С: **вы забираете**, мы отдаём.

### 7.1. Забрать очередь

```
GET /v1/integrations/1c/orders?limit=200
```

```json
{ "orders": [{
  "orderId": "MATA-581431",
  "createdAt": "2026-09-02T11:31:57+00:00",
  "customer": { "phone": "+79148278470", "name": "Михаил", "email": "m@example.com" },
  "items": [
    { "id": "GUID-1", "article": "AR-X1", "productId": "p1",
      "name": "Кроссовки Air Runner X1", "size": "42", "color": "Чёрный",
      "qty": 1, "price": 11990 }
  ],
  "total": 11990,
  "deliveryCost": 0,
  "pointsRedeemed": 100,
  "payment": "online",
  "paymentStatus": "paid",
  "delivery": "courier",
  "address": "Якутск, Ленина, 1, 5",
  "postalCode": ""
} ] }
```

В позиции есть и `id`, и `article` — подставляем их из карточки товара, чтобы вам
не пришлось сопоставлять позиции по названию.

**В очередь попадают только заказы, готовые к сборке** — оплаченные либо те, где
оплата не требуется. Заказ, ожидающий оплаты, мы не отдаём: иначе у вас копятся
брошенные корзины.

### 7.2. Подтвердить приём — обязательно

```
POST /v1/integrations/1c/orders/ack
{ "orderIds": ["MATA-581431"] }        → { "acked": 1, "unknown": [] }
```

**Заказ остаётся в очереди, пока не придёт подтверждение.** Создали документ у себя —
подтвердите. Если связь оборвалась и подтверждение не дошло, заказ просто придёт
в следующей выборке: лучше лишний запрос, чем потерянный заказ. Повторное
подтверждение ошибкой не считается.

### 7.3. Вернуть статус

```
POST /v1/integrations/1c/orders/status
{ "orders": [{ "orderId": "MATA-581431", "status": "shipped", "number": "УТ-000123" }] }
```

| `status` | Что это | Что делает у нас |
|---|---|---|
| `accepted` | принят в 1С | покупатель видит этап, общий статус не меняется |
| `assembled` | собран | покупатель видит этап, общий статус не меняется |
| `shipped` | отгружен | заказ переходит в «Отправлен» |
| `delivered` | доставлен | заказ переходит в «Доставлен» |
| `canceled` | отменён | заказ переходит в «Отменён» |

`number` — ваш номер документа, он попадает в карточку заказа у владельца, это
сильно помогает при разборе обращений.

По каждому статусу покупатель получает уведомление в приложении. **Один и тот же
статус, присланный повторно, ничего не меняет и второе уведомление не шлёт** —
можно спокойно перепосылать.

---

## 8. Право вето владельца — почему цена может «не примениться»

Шесть полей владелец магазина может вести вручную: **цена, старая цена, описание,
размеры, цвета, фото**. Если он поправил поле в конструкторе витрины, следующая
выгрузка это поле **не перезаписывает** — оно приходит в отчёте в `keptByOwner`.
Значение из 1С мы всё равно сохраняем и показываем владельцу рядом, у него есть
кнопка «вернуть как в 1С».

Это не ошибка обмена. Если видите поле в `keptByOwner` — значит владелец сознательно
ведёт его сам.

**Остаток — исключение: он всегда берётся из 1С** и переопределению не подлежит.
Показать размер, которого нет на складе, дороже любого удобства.

---

## 9. Регламент

| Параметр | Значение |
|---|---|
| Категории и карточки | по изменению или раз в час |
| Цены и остатки | раз в 5–15 минут |
| Заказы (забрать) | раз в 5–15 минут |
| Тип выгрузки | дельта по `updatedAt` + раз в сутки полная сверка |
| Размер одной выгрузки | до 20 000 позиций (больше — `413`, слать частями) |
| Идемпотентность | по ключу `id` / `article`, дубли не создаются |
| Снятие с продажи | `active: false`, не удаление из выгрузки |

---

## 10. Что из 1С присылать НЕ нужно

Эти вещи ведёт магазин. Если прислать — перезатрёте настройки витрины:

- маркетинговые флаги: «Новинка», «Рекомендуемое», «Опубликован на витрине»;
- рейтинг и отзывы товара — их пишут покупатели;
- баллы лояльности, их начисление и списание;
- баннеры, тексты и оформление сайта и приложения.

---

## 11. Как проверить, что всё работает

1. `GET /v1/integrations/1c/status` → `{"status":"ok","enabled":true}` — связь есть,
   приём включён.
2. Отправить одну категорию и один товар, посмотреть отчёт в ответе.
3. Попросить владельца открыть **админку → Мониторинг → «Журнал обмена с 1С»**: там
   видно каждую выгрузку, её статус и замечания. Если 1С молчит больше суток,
   страница об этом предупреждает.

---

## 12. Открытые вопросы к вам

1. **Идентификатор позиции:** GUID номенклатуры или артикул? И то же для вариантов
   (размер/цвет).
2. **Где лежат фото** и доступны ли ссылки из интернета без авторизации.
3. **Остаток:** сможете сразу по размерам (`variants`) или на первом этапе общий.
4. **Расписание** ваших регламентных заданий по каждому из четырёх потоков.
5. **Кто дежурный** с вашей стороны, если обмен встанет.

---

## 13. Что осталось на потом (этап 2+)

Ни одно из этого не блокирует запуск и делается, когда понадобится:

- остаток по **нескольким складам** и точкам;
- **резервирование** остатка под оформленный заказ;
- штрихкоды, вес и габариты (для расчёта доставки);
- типы цен, скидки и акции по правилам 1С;
- возвраты и обмены.

---

### Одной фразой

> Дайте каждой позиции **неизменный id** и по нему присылайте **категорию,
> наименование, цену, остаток числом, размеры, цвета, фото и признак «активен»**.
> Забирайте заказы, подтверждайте приём и возвращайте статус. Всё остальное —
> витрина, отзывы, баллы, оформление — магазин ведёт сам.
