# AGENTS.md — указатель

Все правила работы с этим монорепо живут в **`CLAUDE.md`** (корень репозитория) —
прочитай его перед любой работой.

Единственный ИИ-агент проекта — **Claude** (решение владельца от 2026-08-14; Codex
отключён). Этот файл сохранён как указатель для инструментов, читающих конвенцию
AGENTS.md, и больше не является зеркалом правил.

Критичный минимум, если читаешь только этот файл:
- `main` защищён — только ветка → Pull Request → зелёный CI → squash.
- Перед работой: `git checkout main && git pull`, прочитать `docs/WORKLOG.md`
  (свежие записи) и весь `docs/PITFALLS.md`. После работы — дополнить `docs/WORKLOG.md`.
- Репозиторий публичный — никаких секретов/PII в коммитах.
- Дизайн/бренд не трогать без команды владельца.
