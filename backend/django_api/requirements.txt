# 5.2 — LTS. Верхняя граница <5.2 держала нас на 5.1.x, куда исправления уязвимостей
# уже не приезжают: pip-audit нашёл 6 CVE, закрытых в 5.2.16 (D-38).
Django>=5.2.16,<6.0
djangorestframework>=3.15
sqlparse>=0.6.0  # транзитивная (Django); 4 CVE закрыты в 0.6.0 — фиксируем явно
psycopg[binary]>=3.1
django-cors-headers>=4.3
gunicorn>=22.0
Pillow>=10.0
django-unfold>=0.40
sentry-sdk[django]>=2.0
redis>=5.0  # общий кэш в проде (rate-limit/OTP/бан), активен при REDIS_URL
celery>=5.3  # фоновые задачи/очереди/beat (D-07); без брокера — EAGER (синхронно inline)
django-storages[boto3]>=1.14  # медиа в S3/Object Storage (D-31); активно при MEDIA_S3_* (иначе локальный диск)
imageio-ffmpeg>=0.4  # ffmpeg-бинарник: авто-сжатие загруженных фоновых видео в web-формат
