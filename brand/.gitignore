# Локальная библиотека бренда МАТА (исходники от mudreza).
# Репозиторий ПУБЛИЧНЫЙ — сюда НЕ коммитим бренд-кит целиком (логотипы-исходники,
# брендбук, элементы). Файлы лежат локально, берём отсюда нужный и кладём в продукт —
# и коммитим ТОЛЬКО тот конкретный экспортированный файл в assets продукта.
#
# Игнорируем всё в папке, кроме README и самого .gitignore.
*
!.gitignore
!README.md
